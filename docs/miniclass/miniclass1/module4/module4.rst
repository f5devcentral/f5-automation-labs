Module 4 – Puppet automation with f5_command
============================================

F5_command module allows you to send an arbitrary command to an BIG-IP node. It
provides a way to execute native tmsh or bash commands, or a workaround for 
those BIG-IP resources we still yet to develop the provider(s)

.. toctree::
   :maxdepth: 1
   :glob:

   lab*